#!/bin/bash
# Dependency: paste.exe from https://www.c3scripts.com/tutorials/msdos/paste.html
# Alternatively: `choco install pasteboard` provides pbpaste.exe, but a little slower
paste.exe | sed 's/\r$//' | sed -z '$ s/\n$//'
exit 0
