#!/opt/homebrew/bin/bash

SCRIPT_DIR="$(dirname "$0")"
export PATH="/opt/homebrew/bin:$SCRIPT_DIR:$PATH"

if pgrep -xq AeroSpace; then
    exec aerospace-set "$@"
elif pgrep -xq yabai; then
    exec yabai-set "$@"
else
    echo "No window manager running" >&2
    exit 1
fi
