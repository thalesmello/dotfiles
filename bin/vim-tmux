#!/bin/sh
if [ -z $TMUX ]; then
       nvim -u ~/.litevimrc $@
else
       tmux split-window "
              trap \"tmux wait-for -S p12346-neww-done\" 0
              nvim -u ~/.litevimrc $@
              " \; wait-for p12346-neww-done
fi

