return {
  setup = function(ctx)
    -- Override or add bindings here. Example:
    -- ctx.default:bindOnce(ctx.hyper, "e", "Focus My App", function()
    --   ctx.shell('wm-preset focus-app "My Custom App"')
    -- end)
  end,
}
